export default function Size({ id }) {
    return (
    <button
        type="button"
        className="w-fit h-10 rounded-full bg-[#f0f0f0] px-5 pt-2 pb-5"
    >
        {id}
    </button>
    );
}
